## 项目文件说明

`Model.py`:模型定义文件

`train.py`:存放训练相关代码

`test.py`:存放预测正确率相关代码。为了使上传的作业包最小化，没有保留预训练数据，因此在运行`test.py`输出预测结果前，请先运行`train.py`

`./data`文件夹：存放数据集，为了使上传的作业包最小化，里面不含数据集文件。请先运行`train.py`

`./models`文件夹：存放checkpoint处保存的模型参数文件，为使上传的作业包最小化，里面不含参数文件，请先运行`train.py`

`requirements.txt`:依赖

